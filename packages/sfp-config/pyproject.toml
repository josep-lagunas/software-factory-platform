[project]
name = "sfp-config"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["pydantic-settings>=2.5.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.run]
source = ["src/sfp_config"]

[tool.coverage.report]
fail_under = 90
