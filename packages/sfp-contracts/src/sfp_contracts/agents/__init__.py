"""Agent contracts: the inter-agent message envelope and status enum.

This package is intentionally minimal at this stage (SFP-30). Typed per-agent
payloads (planner/coder/reviewer/test-designer) are introduced by SFP-14/15/16/17;
re-exports are deferred until downstream consumers exist to avoid premature
coupling.
"""
