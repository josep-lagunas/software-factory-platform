[project]
name = "sfp-observability"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["structlog>=24.4.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.run]
source = ["src/sfp_observability"]

[tool.coverage.report]
fail_under = 90
