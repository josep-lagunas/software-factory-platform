[project]
name = "identity"
version = "0.1.0"
requires-python = ">=3.13,<3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.run]
source = ["src/identity"]

[tool.coverage.report]
fail_under = 90
