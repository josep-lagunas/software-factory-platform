[project]
name = "orchestrator"
version = "0.1.0"
requires-python = ">=3.13,<3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.run]
source = ["src/orchestrator"]

[tool.coverage.report]
fail_under = 90
