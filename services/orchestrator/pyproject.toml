[project]
name = "orchestrator"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",
    "pydantic>=2.0.0",
    "sfp-contracts",
    "sfp-messaging",
    "sfp-agent-runtime",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Workspace members resolved locally, never from a registry (SFP-75 precedent).
# sfp-contracts: WorkflowUpdated event + envelopes (SFP-137 publishing wrapper).
# sfp-messaging: the MessageBus interface seam (DOC#42; in-memory transport
# today per the software-first owner decision, SFP-101 re-plumbs later).
# sfp-agent-runtime: the vendor-neutral AgentRuntime / PromptProvider Protocol
# seam (SFP-149 — the ReadinessGateHost runs the L2 model through it; the
# orchestrator never imports a vendor SDK above this seam, AP-010 / MAS §9.6).
[tool.uv.sources]
sfp-contracts = { workspace = true }
sfp-messaging = { workspace = true }
sfp-agent-runtime = { workspace = true }

[tool.coverage.run]
omit = ['*/migrations/env.py']
source = ["src/orchestrator"]

[tool.coverage.report]
fail_under = 90
